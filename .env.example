# Environment Configuration Example
# Copy this file to .env.local for local overrides if needed.
# Note: The core portfolio and Prasanna AI operate deterministically without external API keys.

# Optional: Public site domain configuration
NEXT_PUBLIC_SITE_URL=https://prasannaraj.dev

# Optional: External LLM provider API keys (server-side only, do not expose in client)
# OPENAI_API_KEY=
# GEMINI_API_KEY=
